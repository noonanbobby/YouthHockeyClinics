# ══════════════════════════════════════════════════════════════
# NOONAN HOCKEY — Environment Variables
# ══════════════════════════════════════════════════════════════
# Copy this file to .env.local and fill in your values.
# NEVER commit .env.local to git (it is already in .gitignore).
#
# For Vercel: Dashboard → Project → Settings → Environment Variables
# Set each variable for Production, Preview, and Development.

# ── Admin ──────────────────────────────────────────────────────────────────────
# Comma-separated list of email addresses that have admin access.
ADMIN_EMAILS=

# ── Authentication (NextAuth.js) ───────────────────────────────────────────────
# Generate NEXTAUTH_SECRET with: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL=https://your-domain.vercel.app

# Google OAuth — console.cloud.google.com → APIs & Services → Credentials
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Apple Sign In — developer.apple.com (requires $99/yr Apple Developer Program)
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=

# ── Credential Encryption ──────────────────────────────────────────────────────
# Used to AES-256-GCM encrypt DaySmart and IceHockeyPro passwords before they
# are stored in Supabase. NEVER expose this value to the client.
#
# Generate with: openssl rand -hex 32
# Result must be exactly 64 hex characters (32 bytes = 256-bit AES key).
#
# WARNING: If you rotate this key, all previously encrypted credentials become
# unreadable. Store a backup in a secrets manager (1Password, AWS Secrets Manager).
CREDENTIAL_ENCRYPTION_KEY=

# ── Search APIs (server-side ONLY — NEVER prefix with NEXT_PUBLIC_) ────────────
# These keys are read exclusively in src/app/api/search/route.ts on the server.
# They are never sent to the browser under any circumstances.

# Google Programmable Search Engine
# Setup: console.cloud.google.com → Enable "Custom Search API" → Create credentials
# CSE:   programmablesearchengine.google.com → Create search engine → Get CSE ID
GOOGLE_API_KEY=
GOOGLE_CSE_ID=

# Brave Search API — brave.com/search/api (2,000 free queries/month)
BRAVE_API_KEY=

# Tavily AI Search — tavily.com (1,000 free queries/month)
TAVILY_API_KEY=

# Eventbrite — eventbrite.com/platform/api
EVENTBRITE_API_KEY=

# ── Supabase (Cross-Device Sync & Data Storage) ────────────────────────────────
# Create a free project at supabase.com
# Find these values at: Project Settings → API

# Public URL and anon key — safe to expose to the browser (NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — server-side only, has full DB access, NEVER expose to client
SUPABASE_SERVICE_ROLE_KEY=

# ── Supabase RLS Setup ─────────────────────────────────────────────────────────
# After creating your Supabase project, run the SQL in
# src/supabase/migrations/001_user_settings_rls.sql
# in the Supabase SQL Editor to lock down Row Level Security on all tables.
# This ensures each user can only read and write their own data.

# ── DaySmart Integration ───────────────────────────────────────────────────────
# No server-side env vars needed — credentials are entered by users in the
# Integrations UI, encrypted with CREDENTIAL_ENCRYPTION_KEY, and stored in Supabase.
# Scraper: POST /api/integrations/daysmart

# ── IceHockeyPro Integration ───────────────────────────────────────────────────
# Same as DaySmart — user-entered credentials, encrypted at rest.
# Scraper: POST /api/integrations/icehockeypro

# ── Max Ivanov Integration ─────────────────────────────────────────────────────
# No credentials needed — public site scraping.
# Scraper: POST /api/integrations/icehockeypro?action=camps

# ── Optional: Analytics ────────────────────────────────────────────────────────
# Vercel Analytics is enabled automatically on Vercel deployments.
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# ── Optional: Error Monitoring ─────────────────────────────────────────────────
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_AUTH_TOKEN=
# SENTRY_ORG=
# SENTRY_PROJECT=
